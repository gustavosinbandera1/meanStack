'use strict';

// Configuring the new module
angular.module('materiaprimas').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		
		/*Menus.addMenuItem('topbar', 'Materia prima', 'materiaprimas', 'dropdown', '/materiaprimas(/create)?');
		Menus.addSubMenuItem('topbar', 'materiaprimas', 'Listar Materia prima', 'materiaprimas');
		Menus.addSubMenuItem('topbar', 'materiaprimas', 'Nueva Materia prima', 'materiaprimas/create');
	*/
	}
]);
